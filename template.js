Teams.template = [
  function(api, my_storage, shared_storage) {
    // NW hunter
  },
  function(api, my_storage, shared_storage) {
    // NE hunter
  },
  function(api, my_storage, shared_storage) {
    // SE hunter
  },
  function(api, my_storage, shared_storage) {
    // SW hunter
  }
]

